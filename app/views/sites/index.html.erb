<div class="container-fluid">
	<h1>Sites</h1>

	<% if @sites.any? %>
		<ul>
			<% @sites.each do |site|%>
				<li><%= site.site_name %></li>
			<% end %>
		</ul>
	<% else %>
		<p>There are no sites yet!</p>
	<% end %>
</div>